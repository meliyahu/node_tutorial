module.exports = {
    counter: (arr) => {
        return 'There are ' + arr.length + ' in this array';
    },
    adder: (a, b) => {
        return `Sum of ${a} and ${b} = ${a+b}`;
    },
    pi: 3.142
}